# Execute Progress — DTW Order (Tenant / Merchant)

Spec: `ai_specs/0002-tenant-merchant-app/spec.md`
Branch: `feat/busboy-order-app` (layered on top of the uncommitted busboy build)
Mode: **no-commit, no-push** (per-item snapshots are `git write-tree` tree hashes; resume relies on hashes recorded below, not git log).
Tenant run base tree: `a2f54fa`

## Run decisions
- UI-first with **mock data + stubbed Riverpod providers** behind `// TODO(open-question)`. No invented backend.
- **Flavor split**: add a parallel `main_tenant.dart` entrypoint + tenant router/shell; **do NOT refactor or break the busboy app / its tests**.
- **Reuse existing shared widgets**: `lib/core/widgets/` AppInput, PrimaryButton, SegmentedTabBar, SuccessModal; theme `app_theme.dart`; icons `obra_icons`.
- Design-note requirements (L6) are design intent to BUILD into the menu/variant UI (PIN/Popular, discount fields, no stock, no promo, options required+multi-select+"+price"); their data/validation specifics stay Open Questions (stubbed).
- Cache: `.ftk/figma/CPWCfPomucIUq4k7hZwtwM-tenant/` (34 screens, 57 flow edges, 40 assets; `laporan` ref is @0.5x).

## Order (topological)
01 → 02 → 03 → 05 → 08 → 04 → 06 → 07

## Ledger
<!-- one line per completed item: <item>: complete (<baseTree7>..<headTree7>, verdicts — Minors deferred) -->
01 tenant-shell: complete (ca5a18d..7efd72a, spec PASS / quality Approved) — Minors: (a) nav bar visual diverges from menu-order-baru reference (ref = raised-FAB Order + flat labels Menu/Laporan/Akun w/ list/grid/chart/person icons; built = flat Order/Menu-Saya/Laporan/Admin w/ obra home/search/bar_chart/message) — RECONCILE in item 04 (Orders) + update tenant_shell.dart to match reference nav; (b) post-login transition unverified (login is placeholder). Flavor mechanism: lib/core/flavor.dart AppFlavor enum + appFlavorProvider (default busboy); app.dart switches router by flavor; bootstrap({overrides}); main_tenant.dart overrides to tenant. Busboy 71 tests unchanged.

## Tenant route map (item 01 — later items fill each route's builder only; names/paths stable)
Provider: tenantRouterProvider (lib/core/router/tenant_router.dart); constants: TenantRoutes.*; shell: lib/core/widgets/tenant_shell.dart (StatefulShellRoute, 4 branches).
- OUTSIDE shell: tenantLogin /login (login-default), tenantLoginTenant /login/tenant (login-tenantt)
- Order branch: tenantOrder /order (menu-order-baru), tenantOrderDetail /order/baru-2 (menu-order-baru-2), tenantMenuDiproses /order/diproses, tenantPesananDiproses /order/pesanan-diproses, tenantPesananDitolak /order/ditolak, tenantKonfirmasiPesanan /order/konfirmasi, tenantAlasanPenolakan /order/alasan-penolakan (modal), tenantSelesai /order/selesai, tenantPesananBerhasil /order/pesanan-berhasil (berhasil-ditambahkan-2 modal)
- Menu Saya branch: tenantMenuSaya /menu-saya, tenantMenuSayaV2 /menu-saya/v2, tenantMenuBerhasil /menu-saya/berhasil (menu-berhasil-ditambahkan), tenantTambahMenu /menu-saya/tambah, tenantMenuDiisi /menu-saya/diisi, tenantKelolaMenu /menu-saya/kelola (modal), tenantKelolaMenu2 /menu-saya/kelola-2 (modal), tenantBerhasilDitambahkan /menu-saya/berhasil-modal (modal), tenantKelolaVarian /menu-saya/varian, tenantVarianDitambahkan /menu-saya/varian-ditambahkan, tenantVarianDisimpan /menu-saya/varian-disimpan, tenantTambahVarian /menu-saya/tambah-varian, tenantTambahVarian2 /menu-saya/tambah-varian-2, tenantVarianDiisi /menu-saya/varian-diisi, tenantTambahOpsi2 /menu-saya/tambah-opsi-2, tenantOpsi2Ditambahkan /menu-saya/opsi-2-ditambahkan, tenantOpsiVarian1 /menu-saya/opsi-varian-1 (modal), tenantOpsiVarian1Diisi /menu-saya/opsi-varian-1-diisi (modal), tenantOpsiVarian2Diisi /menu-saya/opsi-varian-2-diisi (modal), tenantOpsiVarian2Diisi2 /menu-saya/opsi-varian-2-diisi-2 (modal)
- Laporan branch: tenantLaporan /laporan
- Admin branch: tenantAdmin /admin (admin-offline), tenantAdminOnline /admin/online

02 shared-widgets: complete (7a66e3f..83b3e0f, spec PASS / quality Approved) — Minor: custom pill buttons (_PillButton, _actionButton) re-implement PrimaryButton green pill (legit: outlined-red/half-width/icon variants PrimaryButton can't do; defer to a design-system extension). lib/features/tenant/presentation/widgets/ + lib/core/widgets/app_toggle.dart. APIs:
- incoming_order_card.dart: IncomingOrderCard({data:IncomingOrderData, onTap?, onAccept?, onReject?, onPickupReady?, acceptLabel?}); enum IncomingOrderStatus{baru,diproses,selesai}; OrderLineItem{name,price,qty,available,imageUrl?,copyWith}; IncomingOrderData{orderId,tableName,time,status,items,total,note?}; OrderItemAvailabilityRow({item, onAvailabilityChanged}) = per-item reject.
- menu_item_card.dart: MenuItemCard({data:MenuItemData, onActiveChanged?, onTap?}); MenuItemData{name,price,originalPrice?,popular,stockLabel,active,imageUrl?} (popular→Populer badge; originalPrice→struck-through).
- variant_rows.dart: VariantSelectRow({data:VariantSelectData, selected, onSelectedChanged}); enum VariantType{tunggal,ganda}; VariantRuleToggleRow({title,subtitle,value,onChanged}) (2 instances = required + multi-select); VariantOptionRow({data:VariantOptionData, onRemove?, showReorderHandle}); VariantOptionData{name,addonPrice?} (null→Gratis else +price).
- online_status_toggle.dart: OnlineStatusToggle({online, onToggle, onlineDescription?, offlineDescription?}) — full Status Tenant card; onToggle(!online).
- core/widgets/app_toggle.dart: AppToggle({value, onChanged?, semanticLabel?}) green pill switch. Added theme toggleTrackOff(#E4E7EC).
Note: OnlineStatusToggle is the full card, not the small header status pill (item 05 can request separately).
03 login-tenant: complete (82f0470..28bedbc, spec PASS / quality Approved) — Minors: (a) cross-feature import RoleCard/LoginStatusBar/LoginRole from features/auth into features/tenant (relocate to core/widgets later); (b) self-golden hero-image decode race — tenant_login_default.png hero blank vs tenantt shows it (precacheImage before pump); (c) login-tenantt Pilih Tenant is empty disabled stub (ref shows Starbucks populated — allowed). Built DEDICATED lib/features/tenant/presentation/screens/tenant_login_screen.dart (tenant differs: Pilih Tenant dropdown, "Tenant" copy, Tenan pre-selected). Reuses RoleCard/LoginStatusBar (auth) + AppInput/PrimaryButton (core). Router: /login + /login/tenant → TenantLoginScreen; role-tap→tenantLoginTenant, Masuk→tenantOrder. Busboy source untouched.
05 admin-status: complete (536c6a6..bf37cf6, spec PASS / quality Approved) — built across 2 sessions (1st interrupted mid-way, 2nd finished: fixed lint + regenerated stale .g.dart hash). Minor: golden asset-load nondeterminism — admin_offline.png hero blank vs admin_online.png shows it (precacheImage before pump). [RECURRING: same issue as item 03 — SYSTEMIC precacheImage fix for final review.] One AdminStatusScreen (ConsumerWidget) drives both via @riverpod adminOnlineStatusProvider(initialOnline:) family (in-place flip, no nav); /admin→false, /admin/online→true. Reuses OnlineStatusToggle. Mock @riverpod AdminOnlineStatus + tenantAdminInfo (TODO). admin-hero.png bundled. Cards: Status Tenant, Jam Operasional, Informasi Tenant. 114 tests pass.
08 laporan: complete (a3d0723..dee2765, spec PASS / quality Approved) — Minor: date label ("Selasa, 13 Mei 2026") rendered at bottom of scroll instead of in header (ref shows it under tenant name above filter chips; frame is layoutMode NONE so tree order misleads — move dateLabel into LaporanHeader). lib/features/tenant/ laporan: screen + widgets (laporan_header/summary/charts/lists, report_common) + laporan_report.dart models + @riverpod laporanReport mock (TODO). Charts = CustomPaint (LineChartView x2, HourlyOrdersChart, donut) — no raster/pubspec change. Reused busboy Performa SectionCard + AppColors chart accents; re-expressed InsightCard/DeltaPill as ReportCalloutCard/TrendPill (decoupled). Sections: header+Export+filter chips, hero Total Pendapatan, 2x2 metrics, Grafik Penjualan, Jam Ramai, Menu Terlaris, Stok Menipis, Performa Menu donut, Jam Ramai/Rata-rata bars, Tren Penjualan, Insight Otomatis. 117 tests pass.
04 orders-flow: complete (a1998a0..post-fix, spec PASS / quality Approved after fix) — built in 2 dispatches (A: order board + provider + nav reconcile; B: reject flow) after 2 API-timeout deaths on the monolithic dispatch (reset+decomposed). Important fix applied: reject_confirmed_modal now REUSES showSuccessModal (was duplicating SuccessModal scaffold). Reviewer finding #2 (frames are courier not tenant / missing refs) VERIFIED FALSE — menu-order-baru/tree.txt shows tenant order mgmt (KFC, #92842 Baru, Tolak/Terima, nav Order/Menu/Laporan/Akun); all refs present. Minors deferred: menu-order-baru-2 treated as duplicate of order home (delegates); confetti omitted on berhasil modal; deep-link modal routes hardcode sample data (TODO). @riverpod TenantOrderBoard (accept/markReady/reject, mock). Screens: TenantOrderScreen (in-place baru/diproses/selesai via SegmentedTabBar), TenantRejectOrderScreen (pesanan-ditolak + konfirmasi via OrderItemAvailabilityRow per-item + reject_reason_sheet → konfirmasi), TenantOrderDetailScreen. Nav reconciled: raised-FAB Order + Menu/Laporan/Akun (obra receipt/task_list/bar_chart/user); busboy app_shell untouched. 140 tests pass.
06 menu-mgmt: complete (7dfb36e..post-fix, spec PASS / quality Approved after fix). Important fix applied: search field + Nama Menu now REUSE AppInput (were bespoke boxes). Minors deferred: MenuSuccessModal uses bespoke Dialog+confetti (JUSTIFIED — confetti/auto-dismiss/no-CTA don't fit SuccessModal); menu_provider setActive rebuilds field-by-field (add copyWith); every list row onTap→menuDiisi (mock). lib/features/tenant/. @riverpod MenuList (add/setActive mock). Screens: menu_saya_screen (list: header+AppInput search+filter pills+MenuItemCard), tambah_menu_screen (form: photo, Nama[AppInput], Kategori/Tag dropdowns, Harga Rp-addon, Diskon card [% + Harga Diskon + Berlaku Sampai], Populer PIN toggle, Varian card, Simpan). L6 applied: promo+stock removed, best-seller→Populer(PIN), 2 discount fields+date. kelola_menu_sheet, menu_success_modal. Bespoke boxes kept for price-addon/dropdown/date/multiline (AppInput can't express). 'add variant'→tenantKelolaVarian; other variant routes still placeholder. 151 tests pass.
07 variant-mgmt: complete (c9efe34..9d92968, spec PASS / quality Approved) — built in 2 dispatches (A: variant screens+provider; B: option modals). Minors deferred: opsi_varian_modal _NamaField duplicates AppInput (use AppInput; _HargaField justified for Rp-addon); deep-link modal 1-frame flash (recurring pattern). @riverpod VariantList (add/loadSaved/addOption mock). Screens: KelolaVarianScreen (kelola-varian empty + varian-disimpan), PilihVarianScreen (picker via VariantSelectRow), TambahVarianScreen (form: Nama + 2 VariantRuleToggleRows [Wajib Dipilih+Pilih Lebih dari Satu = required+multi-select L6] + VariantOptionRows + Simpan). opsi_varian_modal (358-wide add-option: Nama + Rp-addon +price → VariantOptionData null/0→Gratis else +Rp). All 12 variant/option routes wired. varian-ditambahkan reuses L6 TambahMenuScreen w/ attached variants. 179 tests pass.

## ALL 8 TENANT ITEMS COMPLETE — 179 tests pass, analyze clean, busboy green throughout.

## FINAL WHOLE-RUN REVIEW: READY (run base a2f54fa .. 054918f)
- analyze 0 issues; flutter test 179/179 pass; build_runner 0 drift; all 34 tenant frames reachable; login outside tenant shell; 4 tabs correct; 0 PlaceholderScreen remaining.
- BUSBOY ISOLATED: only lib/app.dart (flavor switch, default busboy) + lib/bootstrap.dart (optional overrides) touched — backward-compatible; main_dev/main_prod still boot busboy; busboy 71 tests + goldens unchanged. app_theme additive-only.
- L6 confirmed: menu form Populer/PIN + 2 discount fields + promo/stock removed; order reject-per-item (rejectedItemNames+reason); variant required+multi-select+"+price".
- #1 follow-up recommended: SYSTEMIC precacheImage-before-pump golden stability (recurring 03/05/08 image-decode nondeterminism + deep-link 1-frame flashes) — latent flakiness, not failing now.
- Other Minors all deferred (mock-stage): 02 pill buttons, 03 cross-feature import + Pilih Tenant stub, 06 MenuSuccessModal bespoke (justified) + copyWith + row onTap, 07 _NamaField dup + deep-link flash, 08 date label position, deep-link wrappers hardcode sample data.

## TENANT RUN COMPLETE. No commit / no push (per user). Working tree on branch feat/busboy-order-app now holds BOTH the busboy build (spec 0001) and the tenant build (spec 0002), all uncommitted.
